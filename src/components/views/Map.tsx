import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  flex: 1;
  height: 100%;
`;

const MapView = () => {
  return <Wrapper>MapView</Wrapper>;
};

export default MapView;
